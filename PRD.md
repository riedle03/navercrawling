# PRD — 슬기로운 말글마이너: 네이버 뉴스편

**버전**: 1.1
**작성일**: 2026-03-15
**수정일**: 2026-03-15
**작성자**: 이대형
**참조 프로젝트**: 슬기로운 말글마이너: YouTube 댓글편 (riedle03.github.io/youtubecrawling)

---

## 1. 제품 개요

### 1.1 한 줄 정의
네이버 뉴스 검색 API로 수집한 기사 제목+발췌문을 바른(Bareun) 형태소 분석으로 텍스트마이닝하는 **교육용 Vanilla JS SPA**.

### 1.2 시리즈 맥락
```
슬기로운 말글마이너: YouTube 댓글편  → 대중의 반응 분석 (구어체)
슬기로운 말글마이너: 네이버 뉴스편  → 언론의 표현 방식 분석 (문어체)  ← 이번
```
두 도구를 같은 키워드로 비교하는 미디어 리터러시 수업 설계 가능.

### 1.3 대상 학습자
- **주 대상**: 중학생·고등학생
- **확장 대상**: 초등 고학년, 교사
- 디지털 리터러시 기초 수준 가정 (스마트폰·PC 사용 가능, 코딩 지식 불필요)
- API 키 발급은 교사가 대신하거나 단계별 안내로 학생 직접 수행 가능

### 1.4 교육 목적
- 특정 이슈에 대해 언론이 어떤 어휘를 사용하는가 분석
- 뉴스 텍스트의 핵심 키워드·연어(共起語) 파악
- 국어·사회 수업에서 미디어 언어 비판적 읽기 실습
- 중복 기사 현상을 통해 광고성·보도자료 기사 문제 체감

---

## 2. 기술 스택

### 2.1 프론트엔드 (YouTube편과 동일)
| 역할 | 라이브러리 |
|------|-----------|
| UI | Vanilla JS + Tailwind CSS CDN |
| 차트 | Chart.js 4.4.0 |
| 워드클라우드 | wordcloud2.js 1.2.2 |
| 네트워크 그래프 | D3.js 7.9.0 |
| 형태소 분석 | 바른(Bareun) API |

### 2.2 백엔드 (신규)
| 역할 | 기술 |
|------|------|
| CORS 프록시 | Vercel Serverless Function (`api/naver.js`) |
| 로컬 개발 프록시 | Node.js Express (`server.js`) |

### 2.3 배포
| 환경 | 플랫폼 | 도메인 |
|------|--------|--------|
| 프로덕션 | Vercel | riedle03.vercel.app/navercrawling (또는 별도) |
| 로컬 개발 | Node.js | localhost:3000 |

---

## 3. 네이버 뉴스 API 제약 및 대응

### 3.1 API 한계
| 항목 | 내용 |
|------|------|
| 수집 데이터 | 제목 + 발췌문(150~250자) — 전문 미제공 |
| 최대 수집량 | 1,000건 (display 100 × start 901) |
| 날짜 필터 | API 파라미터 없음 → 수집 후 클라이언트 필터링 |
| CORS | 브라우저 직접 호출 불가 → Vercel 프록시 필수 |
| 인증 | X-Naver-Client-Id / X-Naver-Client-Secret 헤더 |

### 3.2 중복 기사 문제 및 3단계 자동 제거
연합뉴스·뉴시스 발 배포 기사로 인해 1,000건 중 상당수가 유사 중복.
자동으로 3단계 파이프라인 적용:

```
1단계: 완전 동일 제목 제거 (Set 비교)
2단계: 동일 도메인 쿼터 (기본: 도메인당 최대 5건)
3단계: Jaccard 유사도 ≥ 0.33 제거
       (제목 단어집합 OR 제목+발췌 단어집합 중 하나라도 임계값 이상이면 중복 처리)
```

중복 제거 결과는 Step 1 결과 화면에 통계로 표시.

---

## 4. 6단계 파이프라인

### 파이프라인 개요
```
Step 1 (기사 수집) → Step 2 (전처리) → Step 3 (빈도 분석)
     → Step 4 (막대차트) → Step 5 (워드클라우드) → Step 6 (SNA)
```

Step 2~6은 YouTube편 코드를 그대로 재사용 (`App.state.items[].text`만 사용).

---

## 5. Step 1 — 기사 수집 (핵심 신규 개발)

### 5.1 UI 구성

**검색 설정 카드**
- 검색어 입력 (텍스트 인풋 + Enter 지원)
- 수집 개수: 100 / 300 / 500 / 최대(1000)
- 정렬: 관련도순(sim) / 최신순(date)
- 텍스트 범위: 제목만 / 설명만 / 제목+설명(기본)

**날짜 필터 카드** (수집 후 클라이언트 필터링)
- 시작일 ~ 종료일 (date picker)
- 미설정 시 전체 기간

**중복 제거 설정 카드** (토글로 펼침)
- 2단계 도메인 쿼터 수 조절 (1~20, 기본 5)
- 3단계 Jaccard 임계값 조절 (0.3~0.9, 기본 0.33)

**관련성 필터 카드**
- 관련성 필터 토글 (기본: 활성화)
- 검색어 토큰이 기사 제목 또는 발췌문에 하나라도 포함된 기사만 통과
- 비활성화 시 날짜 필터 통과 기사 전체를 중복 제거에 넘김

### 5.2 수집 결과 표시

#### 요약 통계 카드
```
전체 수집:        1,000건
날짜 필터 후:       850건
관련성 필터 후 🎯:  720건
고유 기사 추정:     377건
```
4단계 파이프라인 각 단계 기사 수와 비율을 카드로 표시.

#### 탭 1 — 전체 보기 (미디어 리터러시 교육용)
- 수집된 전체 기사 테이블 표시
- 중복 의심 기사는 시각적으로 구분 (연한 배지 표시)
- "같은 기사가 이렇게 많구나"를 체감하는 것이 교육 목적

#### 탭 2 — 중복 묶음 보기
- 유사 기사들을 그룹으로 묶어 표시
- 대표 기사 제목 + "유사 기사 N건 ▶" 형태로 펼침
- 어떤 기사가 어디서 복붙됐는지 확인 가능

#### 텍스트마이닝 대상 선택
```
다음 단계에 사용할 기사:
  ◉ 고유 기사만 분석 (377건) ← 기본값·권장
  ○ 전체 기사로 분석 (1,000건)
```

#### 수집 데이터 한계 고지 배너 (UI 상단 고정)
```
📌 이 분석은 기사 전문이 아닌 제목(약 30자)과 요약 발췌문(약 150~250자)을
   기반으로 합니다. 같은 검색어라도 수집 시점에 따라 결과가 달라질 수 있습니다.
```
- Step 1 결과 화면 상단에 항상 표시 (접을 수 없음)
- 교사가 학생에게 "왜 이 단어만 나왔지?"라는 질문을 받을 때 근거 제공

#### 언론사 분포 차트 (신규)
- 수집 결과 요약 카드 하단에 도넛 차트 표시
- 언론사별 기사 수 시각화 (상위 10개 언론사 + 기타)
- 교육 활용: "어떤 언론사의 기사가 분석에 포함됐나?" 미디어 편향 논의 가능
- 고유 기사 선택 시 차트도 연동 업데이트

#### 공통
- 테이블 내 검색 필터
- CSV 저장 (번호, 제목, 발췌, 언론사, 날짜, URL, 중복여부)

### 5.3 데이터 구조 (App.state.items)
```js
// YouTube편 comments → 뉴스편 items (Step 2 이후는 .text만 사용)
{
  text:    string,  // cleanHtml(title + ' ' + description) 또는 선택에 따라
  source:  string,  // 언론사명 (도메인 추출)
  date:    string,  // pubDate 파싱 (YYYY-MM-DD)
  link:    string,  // originallink 우선, 없으면 link
  title:   string,  // 원본 제목 (테이블 표시용)
}
```

### 5.4 HTML 정제
```
<b>키워드</b> → 태그 제거
&lt; &gt; &amp; &quot; &#39; → 엔티티 디코딩
```

### 5.5 API 호출 구조 (CORS 프록시)
```
브라우저 → /api/naver?query=...&display=100&start=1&sort=sim
         → Vercel 함수: X-Naver 헤더 추가 후 네이버 API 호출
         → 응답 JSON 반환
```
로컬: `localhost` 감지 시 `/api/naver` → server.js 프록시 동일 경로

---

## 6. Step 2~6 — 전처리·분석 (YouTube편 재사용)

### 변경 사항
| 파일 | 변경 내용 |
|------|----------|
| `app.js` | `state.comments` → `state.items` 변수명 / 상태바 "댓글" → "기사" |
| `step2_preprocess.js` | 검색어 자동 불용어 로직 추가 + 빈 상태 메시지 텍스트 수정 |
| `assets/stopwords.js` | ✅ YouTube편 + 뉴스편 통합 885개 |
| `assets/normdict.js` | ✅ YouTube편 + 뉴스편 통합 (7개 섹션) |

### 불용어 사전 — `assets/stopwords.js` (885개, 통합 완성)

YouTube 댓글편 사전과 네이버 뉴스편 사전을 병합. 주요 추가 섹션:

| 섹션 | 대표 항목 |
|------|----------|
| 보도 상투어 | 기자, 보도, 밝혔다, 따른, 인한, 이라고, 이라며, 기고, 칼럼, 사설, 사진 |
| 뉴스 시간 표현 | 오는, 이날, 당일, 앞서 |
| HTML 잔류어 | br, amp, lt, gt, quot, nbsp |
| 동사 어간·절단어 | 생활하, 지원하, 겪고, 발생하, 해결하기, 수밖, 않도록 |
| 용언/서술 확장 | 있도록, 가운데, 필요한, 필요하다, 넘어, 만든, 했다, 있었다, 밝혀졌다, 나타났다, 늘었다, 못해 |
| 조사 | 의 |

### 단어 통합 사전 — `assets/normdict.js` (통합 완성)

YouTube 댓글편 normdict와 병합. 7개 섹션:

| 섹션 | 대표 항목 |
|------|----------|
| 정치·행정 | 대통령, 국회, 정부, 여당, 야당 |
| 경제·금융 | 금리, 물가, 주식, 부동산, 경제성장, 전기요금, 자본주의, 빈곤 |
| 노동·취업 | 취업, 재취업, 취업률, 직장, 회사, 직업, 일자리, 알바, 니트족, 노조, 파업 |
| 사회·교육 | 학교, 대학교, 환경, 의료, 복지, 사회, 한국, 한국사회, 외톨이 |
| 은둔·고립 | 은둔, 은둔청년, 고립, 은둔고립 |
| 심리·정신건강 | 정신, 정신과, 병원, 무기력, 우울, 우울증 |
| 기술·산업 | 인공지능(←AI/ai/딥러닝/머신러닝), ChatGPT, 반도체, 전기차, 스마트폰, 숏폼, TV, 현대차, 머스크 |
| 국제·외교 | 미국(←미), 중국(←중), 일본(←일), 북한(←북/조선) |

### 검색어 자동 불용어 처리 타이밍 로직 (안정적 설계)

**핵심 문제**: "기후변화" → 형태소 분석 후 "기후" + "변화"로 분리 → 원형만 불용어 등록 시 분리된 형태가 결과에 남음

**설계 원칙**: 불용어는 본 분석 실행 **이전**에 완전히 확정되어야 함

```
[Step 1] 검색어 → App.state.searchQuery = "기후변화" 저장
                   체크박스 상태 → App.state.autoStopQuery = true/false

[Step 2] runPreprocess() 진입 시 최우선 실행:
  if (autoStopQuery && searchQuery) {
    Phase A: 원형 추가
      → userStopwords에 "기후변화" 추가

    Phase B: 형태소 분리 결과 추가 (모드별)
      → bareun/bareun_josa 모드:
           Bareun API 단독 호출 (검색어 1~3단어, 오버헤드 극소)
           → 추출된 lemma 모두 userStopwords에 추가
           → 예: "기후", "변화" 추가
      → regex 모드:
           cleanAndSplit() + stripParticle() 적용
           → 결과 userStopwords에 추가

    Phase C: UI 반영
      → 불용어 태그 영역에 자동 추가된 단어 표시
      → 사용자가 확인하고 필요시 제거 가능
  }

  → 이후 본 분석(buildLemmaMap) 실행
  → 불용어가 완전히 확정된 상태에서 finalizeResult() 호출
```

**장점**:
- 타이밍 보장 — 불용어 확정 후 분석 시작
- 모든 모드(regex/bareun_josa/bareun) 동일하게 작동
- 투명성 — 자동 추가된 단어가 UI에 보임, 사용자가 수정 가능
- 오버헤드 극소 — 검색어 최대 3~5단어에 대한 Bareun 호출 1회

### 뉴스 특화 불용어 (추가 목록)
```
보도, 기자, 뉴스, 연합, 뉴시스, 기사, 취재, 발표, 관련,
오전, 오후, 지난해, 올해, 올봄, 이번, 해당, 관계자,
밝혔다, 전했다, 말했다, 했다, 됐다, 됩니다, 입니다
```

---

## 7. 설정 모달 변경

### YouTube편 → 네이버 뉴스편
| YouTube편 | 네이버 뉴스편 |
|-----------|-------------|
| YouTube Data API v3 키 (필수) | 네이버 Client ID (필수) |
| — | 네이버 Client Secret (필수) |
| 바른 API 키 (선택) | 바른 API 키 (선택) |

### API 연결 확인 기능 (신규)
설정 저장 시 자동으로 테스트 호출을 수행해 인증 정보가 올바른지 즉시 피드백:
```
[저장] 버튼 클릭
  → 테스트 검색어("뉴스")로 네이버 API 1건 호출
  → 성공: ✅ 연결 확인됨 (초록)
  → 실패: ❌ Client ID 또는 Secret을 다시 확인해주세요 (빨강)
```
중학생·고등학생이 키를 잘못 입력했을 때 즉각적으로 알 수 있도록 함.

### 네이버 API 키 발급 안내 (모달 내)
```
1. developers.naver.com 접속 → 로그인
2. 내 애플리케이션 → 애플리케이션 등록
3. 애플리케이션 이름: 자유롭게 작성
4. 사용 API: 검색 선택
5. 환경: WEB 선택
6. 비로그인 오픈 API 서비스 환경: https://news.naver.com/ 입력
7. 생성된 Client ID / Client Secret 복사
8. 슬기로운 말글마이너: 네이버 뉴스편 설정(⚙️)에 붙여넣기
```
※ 서비스 URL은 항상 https://news.naver.com/ 으로 고정 — Vercel 배포 URL 불필요

---

## 8. 부가 페이지

### 8.1 수업 가이드 (guide.html)
교사용 수업 설계 안내 페이지. YouTube편 guide.html과 동일한 구조.

**구성 섹션**:
```
1. 이 도구는 무엇인가요?
   - 네이버 뉴스 검색 API 기반 텍스트마이닝 설명
   - 발췌문 기반 분석임을 명시

2. 수업 활용 시나리오 (예시 2가지)
   A. 단독 활용: 특정 이슈 뉴스 어휘 분석
      예) "기후변화" 검색 → 언론이 주로 쓰는 단어는?
   B. YouTube편과 비교 활용: 같은 키워드로 두 도구 비교
      예) "기후변화" 댓글 vs 뉴스 → 대중 반응 vs 언론 표현 차이

3. 중복 기사 현상 이해하기
   - 왜 비슷한 기사가 많은가? (연합뉴스 배포 구조)
   - 광고성·보도자료 기사란?
   - 수업 토론 질문 예시

4. API 키 발급 방법 (교사용 단계별 안내)
   - 스크린샷 포함 설명

5. 분석 결과 해석 가이드
   - 워드클라우드 읽는 법
   - SNA(의미연결망) 읽는 법
   - 주의사항: 발췌문 한계

6. 관련 교육과정 연계
   - 국어: 매체 언어, 비판적 읽기
   - 사회: 미디어 리터러시
```

### 8.2 개인정보처리방침 (privacy.html)
YouTube편 privacy.html과 동일한 구조로 작성.

**핵심 내용**:
```
수집하는 정보:
  - 네이버 Client ID / Client Secret → 사용자 브라우저 localStorage에만 저장
  - 바른 API 키 → 사용자 브라우저 localStorage에만 저장
  - 검색어, 분석 결과 → 서버에 저장하지 않음

서버 전송:
  - 검색어만 Vercel 프록시 서버를 경유해 네이버 API로 전달
  - API 키는 Vercel 서버 환경변수가 아닌 사용자 브라우저에서 직접 관리
  - 수집된 기사 데이터는 브라우저 메모리에만 존재

제3자 제공: 없음
데이터 보관: 브라우저 localStorage (사용자가 직접 삭제 가능)
```

---

## 9. 백엔드 파일 명세

### 8.1 `api/naver.js` (Vercel Serverless Function)
```
- 입력: query params (query, display, start, sort)
- 동작: X-Naver-Client-Id / Secret 헤더 추가 후 네이버 API 호출
- 출력: 네이버 API 응답 JSON 그대로 반환
- 환경변수: NAVER_CLIENT_ID, NAVER_CLIENT_SECRET
```

### 8.2 `vercel.json`
```json
{
  "rewrites": [
    { "source": "/api/naver", "destination": "/api/naver.js" }
  ]
}
```

### 8.3 `server.js` (로컬 개발용)
- 기존 server.js에 `/api/naver` 엔드포인트 추가
- `.env` 파일에서 NAVER_CLIENT_ID / SECRET 읽기

---

## 10. 파일 구조

```
navercrawling/
├── index.html                     ← 수정 (타이틀, 설명, 스크립트 목록)
├── guide.html                     ← 신규 (수업 가이드)
├── privacy.html                   ← 신규 (개인정보처리방침)
├── vercel.json                    ← 신규
├── server.js                      ← 신규 (로컬 개발 프록시)
├── .env.example                   ← 신규
├── api/
│   └── naver.js                   ← 신규 (Vercel 서버리스 함수)
├── css/
│   └── style.css                  ← ✅ 완성 (네이버 그린 브랜딩 적용)
├── js/
│   ├── settings.js                ← 수정 (YouTube key → Naver ID/Secret + 연결확인)
│   ├── app.js                     ← 수정 (comments→items, 상태바 텍스트)
│   ├── step1_collector.js         ← 신규 전면 작성 (핵심)
│   ├── step2_preprocess.js        ← 수정 (검색어 자동 불용어 로직 추가)
│   ├── step3_frequency.js         ← 그대로
│   ├── step4_barchart.js          ← 그대로
│   ├── step5_wordcloud.js         ← 그대로
│   ├── step6_sna.js               ← 그대로
│   └── utils.js                   ← 그대로
└── assets/
    ├── wordcloud2.js              ← 그대로
    ├── stopwords.js               ← ✅ 완성 (뉴스+YouTube 통합 885개)
    └── normdict.js                ← ✅ 완성 (뉴스+YouTube 통합 7개 섹션)
```

---

## 11. 개발 순서

```
✅ 사전 완료: css/style.css (네이버 그린 브랜딩)

Phase 1: 백엔드 프록시 (기반 작업)
  1. server.js 작성 + .env.example 작성
  2. api/naver.js 작성 (Vercel 서버리스)
  3. vercel.json 작성
  4. 로컬에서 네이버 API 호출 동작 확인

Phase 2: 핵심 인프라
  ✅ 5. settings.js 수정 (Naver ID/Secret + API 연결 확인 기능)
  ✅ 6. app.js 수정 (comments→items, 상태바 텍스트)
  ✅ 7. stopwords.js / normdict.js — YouTube+뉴스 통합 완성

Phase 3: Step 1 수집 모듈 (핵심)
  ✅ 8. step1_collector.js 작성
     - 검색 UI + 수집 로직
     - HTML 정제 함수 (태그 제거, 엔티티 디코딩, DOMParser)
     - 중복 제거 3단계 파이프라인 (Jaccard 0.33, 제목 OR 전문)
     - 날짜 필터 (클라이언트)
     - 관련성 필터 (검색어 토큰 매칭, 토글 가능)
     - 언론사 분포 도넛 차트
     - 수집 데이터 한계 고지 배너
     - 텍스트마이닝 대상 선택 (전체 / 고유 기사)
     - 통계카드 4단계 (전체→날짜→관련성→고유)

Phase 4: Step 2 수정
  ✅ 9. step2_preprocess.js 수정
     - 검색어 자동 불용어 로직 (Phase A/B/C)
     - 빈 상태 메시지 텍스트 수정

Phase 5: 부가 페이지
  ✅ 10. guide.html 작성 (수업 가이드)
  ✅ 11. privacy.html 작성 (개인정보처리방침)
  ✅ 12. index.html 타이틀/설명/스크립트 수정

Phase 6: 마무리
  13. 전체 통합 테스트 (로컬)
  14. Vercel 배포
  15. 프로덕션 테스트
```

---

## 12. 확정 사항 전체 요약

| 항목 | 결정 |
|------|------|
| 대상 학습자 | ✅ 중고등학생 주 대상, 초등 고학년·교사 확장 |
| API 키 관리 | ✅ 사용자 직접 입력 — localStorage 저장, 서버 전송 없음 |
| API 연결 확인 | ✅ 저장 시 테스트 호출 → 즉각 성공/실패 피드백 |
| 중복 기사 처리 | ✅ 전체 보기(교육용) + 고유 기사만 선택(텍스트마이닝용) 병행 |
| Jaccard 임계값 | ✅ 0.33 (제목 OR 제목+발췌 단어집합 중 하나 이상) |
| 관련성 필터 | ✅ 검색어 토큰 매칭 필터 — 날짜 필터 후, 중복 제거 전 단계로 적용, 토글 가능 |
| 통계카드 파이프라인 | ✅ 전체 수집 → 날짜 필터 후 → 관련성 필터 후 → 고유 기사 (4단계) |
| 검색어 자동 불용어 | ✅ Step 2 진입 시 Bareun API로 검색어 형태소 분석 후 불용어 선등록 |
| 수집 데이터 한계 고지 | ✅ Step 1 결과 상단 배너 (항상 표시) |
| 언론사 분포 차트 | ✅ Step 1 결과에 도넛 차트 추가 |
| 불용어 사전 | ✅ YouTube 댓글편 + 뉴스편 통합 885개 (보도 상투어·HTML 잔류어·동사어간 섹션 신규) |
| 단어 통합 사전 | ✅ YouTube 댓글편 + 뉴스편 통합 (노동·취업, 은둔·고립, 심리·정신건강 섹션 신규) |
| 수업 가이드 | ✅ guide.html 작성 |
| 개인정보처리방침 | ✅ privacy.html 작성 |
| 배포 플랫폼 | ✅ Vercel (CORS 프록시 필요) |

---

## 13. 참조

- 기존 프로젝트: https://github.com/riedle03/youtubecrawling
- 네이버 검색 API 문서: https://developers.naver.com/docs/serviceapi/search/news/news.md
- 바른 API: https://bareun.ai
- Vercel Serverless Functions: https://vercel.com/docs/functions
